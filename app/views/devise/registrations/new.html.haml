.new_session-wrapper
  .new_session-wrapper__logo
    = link_to root_path do
      = image_tag "logo.png", class: "new_session-wrapper__logo__image"

  .new_signup-container
    %h2.new_signup-container__head 会員情報入力
    .new_signup-container__main
      .new_signup-container__main__box
        = form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: true do |f|
          = render "devise/shared/error_messages", resource: resource
          .field
            = f.label :nickname
            %span{class: "require"} 必須
            = f.text_field :nickname, autofocus: true, placeholder: "例）フリマ太郎",class: "new_signup-container__main__box-upper"
          .field
            = f.label :email
            %span{class: "require"} 必須
            = f.email_field :email, autocomplete: "email",  placeholder: "PC・携帯どちらでも可", class: "new_signup-container__main__box-upper"
          .field
            = f.label :password
            %span{class: "require"} 必須
            = f.password_field :password, autocomplete: "new-password", placeholder: "6文字以上の半角英数字", class: "new_signup-container__main__box-upper"
          .field
            = f.label :password_confirmation
            %span{class: "require"} 必須
            = f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "6文字以上の半角英数字", class: "new_signup-container__main__box-upper"
          .field
            = f.label :お名前（全角）
            %span{class: "require"} 必須
            .flexbox
              = f.text_field :firstname, placeholder: "例）山田", class: "new_signup-container__main__box-harf"
              = f.text_field :lastname, placeholder: "例）彩", class: "new_signup-container__main__box-harf"
          .field
            = f.label :お名前カナ（全角）
            %span{class: "require"} 必須
            .flexbox
              = f.text_field :firstname_kana, placeholder: "例）ヤマダ", class: "new_signup-container__main__box-harf"
              = f.text_field :lastname_kana, placeholder: "例）アヤ", class: "new_signup-container__main__box-harf"
          .field
            = f.label :生年月日
            %span{class: "require"} 必須
            .flexbox
              - year = ["--"]
              - for num in 0..110
                - year << 2020-num
              = f.select :birth_year, year, {}, class: "new_signup-container__main__box-three"
              年
              - month = ["--"]
              - for num in 1..12
                - month << num
              = f.select :birth_month, month, {}, class: "new_signup-container__main__box-three"
              月
              - day = ["--"]
              - for num in 1..31
                - day << num
              = f.select :birth_day, day, {}, class: "new_signup-container__main__box-three"
              日
            %p.new_session-wrapper__main__caution 本人情報は正しく入力してください。会員登録後、お時間をいただく場合があります。 
          .actions
            = f.submit "次へ進む", class: "new_signup-container__main__submit"
  .new_signup-footer
